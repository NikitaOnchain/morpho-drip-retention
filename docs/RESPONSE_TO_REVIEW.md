# Response to Independent Review — revised release candidate

Initial review: `reviews/independent/REPORT.md`, 4 September 2026, **CHANGES REQUESTED**. Subsequent independent re-review returned **APPROVE WITH CAVEATS**, accepted by the project owner: **R1 CLOSED; R2 CLOSED; R3 CLOSED; R4 CLOSED**. See [re-review report](../reviews/public/REPORT.md), [verification summary](../reviews/public/verification_summary.json) and [public evidence guide](../reviews/public/README.md). Original reports/evidence remain unchanged; this response records acceptance, not a new reviewer assessment.

Frozen metrics v1.0, production SQLite, accepted numerical CSVs and original Phase 7 PNGs/code remain unchanged. Public derivatives exist in separately built staging. The 5.65M USDC and 4.14M USD₮0 remain full P180 balances, not causal DRIP-created debt or baseline-relative uplift amounts.

| Finding | Package correction | Verification and remaining limits |
|---|---|---|
| R1: missing producer/evidence | Portable producer, Phase 1 metadata manifest and needed Phase 3/4/6 QA restored. `release/derivative_manifest.json` records original/release SHA-256 and path-field transformations. Reviewer hash validation accepts only explicitly mapped derivatives; frozen manifests remain unchanged. The legacy isolated entrypoint requires an explicit bundle. | Exact package audit and accepted-SQLite metric/review reproduction with copied, hash-checked inputs. Public source-bundle access remains unarranged, not PASS. |
| R2: 16 broken links | All compact referenced dependencies and historical receipts restored; old Phase 3 checkpoints labeled historical. No raw/cache/SQLite/private originals returned to the package. | All 16 targets plus all local Markdown file links checked inside staging. See `release/package_audit.json`. External HTTP status, renderer anchors and GitHub rendering untested. |
| R3: incomplete reproduction | [Guide](REPRODUCTION.md) separates CSV-only from full reconstruction. A standalone CSV renderer needs no database. Explicit source-bundle route checks identities, runs the accepted-SQLite metrics and expands complete offline raw commands. | CSV-only and accepted-SQLite routes tested with accidental-dependency guards. Raw identities, Phase 1 regeneration, bridge and four CLI argument sets tested. Full raw → prototype → state rebuild is **NOT RUN in this correction pass**, not end-to-end PASS. |
| R4: sparse post lines | Only active-borrower/archetype presentation derivatives change. Dense lines stop at E; post values are isolated +30/+90/+180 markers with labels and “intermediate trajectory not observed” on-image. | CSVs unchanged; four other PNGs byte-identical. Six figures reproduce from packaged CSVs. Two revised figures visually inspected for labels, units, zero scales and sparse sampling. Browser/GitHub document-render QA remains unperformed. |

The actual payload is enumerated by `release/manifest.json` and `release/SHA256SUMS.txt`. `release/external_inputs.json` lists every excluded source file/hash, including all shards. Compact correction receipts are under `release/qa/`; large private test jobs are outside the package. Python audit-hook tests deny network and undeclared file roots; CSV tests also deny SQLite. This is an accidental-dependency guard, not an OS-level hostile-code sandbox.

Accepted caveats: full raw rebuild/new-DB reconciliation was not run; public source-bundle access is unavailable; browser/GitHub rendering was not checked. R1/R3 closure concerns packaging and honest runnable instructions, not complete raw reconstruction. CSV-only is autonomous with the stated runtime/fonts; the SQLite-route requires the external accepted DB and metadata snapshot.

Remaining publication actions: pseudonym/repository/license choice, final privacy/staged/history checks, final target-renderer review or explicit acceptance of its unverified status, and separate publication permission. Any future source-bundle distribution needs privacy review. No upload, push, external message, new RPC/GBA or Phase 8 closure occurred.

Status: **Phase 8 — CURRENT — reviewed release candidate ready for publication approval**.
