# Phase 8 Release Candidate Preflight

Status: **Phase 8 — CURRENT — reviewed release candidate ready for publication approval**; not approved for publication.

## Accepted re-review and final local packaging — 4 September 2026

The project owner accepted independent **APPROVE WITH CAVEATS**: **R1–R4 CLOSED**. [Portable report](../reviews/public/REPORT.md), [review verification summary](../reviews/public/verification_summary.json) and [public evidence provenance](../reviews/public/evidence_manifest.json) distinguish reviewer evidence from final packaging checks. Original reviewer artifacts and the reviewed RC2 remain unchanged. The final package records its relationship to RC2 in `release/finalization.json`; `release/manifest.json` and `release/SHA256SUMS.txt` enumerate the final payload. Package-local links, checksums and heuristic secrets/path screening are rerun on that payload, not the workspace.

CSV-only needs no DB or source bundle. The separately tested SQLite-route requires two external inputs. Full raw rebuild/new-DB reconciliation **NOT RUN**; public source-bundle acquisition **NOT AVAILABLE**; browser/GitHub rendering **NOT CHECKED**. This is not a self-contained raw-reconstruction package. Dedicated secret/history scanning and final publishing approval are not implied by a zero-hit heuristic scan.

## Historical correction-pass QA — 4 September 2026, before re-review

Independent review confirmed numerical results and returned `CHANGES REQUESTED` for R1–R4. See [response](RESPONSE_TO_REVIEW.md). Portable code/evidence and presentation derivatives map explicitly to original hashes. The actual staging payload is in `release/manifest.json`; `release/package_audit.json` checks package-local paths, including all 16 reported links. Nothing resolves via the original workspace.

CSV-only checks five accepted CSV hashes, ratios/applicability, native deltas, checkpoint values, flow equations and concentration denominators, then reproduces six figures with no DB/network. Accepted-SQLite reproduction matches 648 metric rows, 10,215 share checks and five CSVs using an explicit copied bundle. Raw-preflight checks 3,163 inputs, four complete CLI argument sets, Phase 1 regeneration and the 469-row bridge. Full raw-state rebuild and new-DB logical comparison are **NOT RUN**, not PASS. Compact receipts are under `release/qa/` in staging.

Two revised PNGs were visually inspected: no post connectors, labeled +30/+90/+180 markers, on-image unobserved-trajectory note, readable units and zero scales. Four PNGs retain accepted bytes. Original reviewer evidence and frozen numerical artifacts remain untouched.

The following sections preserve **3 September RC history**, not the revised-package verdict. Historical inventory/hygiene/preflight receipts remain included, but their old projection is not the revised release manifest. Source access, re-review, final browser/GitHub rendering, cross-OS fonts, dedicated/staged/history secret scanning and publication remain open.

## Validation assessment

**Share with caveats for independent review only.** Frozen metric levels and ratios match the accepted production evidence. The local candidate is not yet a privacy-safe, self-contained public package: source access, sanitized derivative packaging, pseudonym/license and independent signoff remain explicit gates.

## Calculation and reproducibility checks

- `scripts/review_phase7_headlines.py` does not import the Phase 7 analysis module. It generates frozen dates independently, reads production state read-only, selects as-of states by binary search, rebuilds wallet borrow shares and recalculates ratios with exact fractions.
- All 648 market/portfolio metric rows and 10,215 market/checkpoint share comparisons match; no numerical/methodological discrepancy was found. See [verification JSON](../data/release/phase8_headline_verification.json).
- `scripts/reproduce_phase7_isolated.py` copied checksum-bound inputs into a retained versioned workspace and reproduced five CSVs and six PNGs byte-for-byte in 58.473 seconds. Accepted inputs and outputs were not changed. See [reproduction JSON](../data/release/phase8_reproduction_qa.json).
- The sole copied metadata relocation was `api.raw_snapshot_path` in the Phase 1 manifest. This is explicitly recorded with before/after hashes; raw contents and all metric definitions/results were preserved.
- Original Phase 6 SQL was not rebuilt or rerun here. Its accepted hashes/QA remain the accounting authority. Python syntax and data-file structure are checked without executing extraction code.

## Release hygiene and links

[Exact inventory](../data/release/phase8_release_inventory.csv) and [redacted locations](../data/release/phase8_hygiene_findings.csv) are generated by `scripts/preflight_phase8_release.py`. `INCLUDE` means a candidate compact file passed the configured local screen; `EXCLUDE` means do not place the original in a public package. Neither means a file was deleted.

Historical code/evidence contains absolute machine-local paths. Those originals remain unchanged and are excluded pending an explicitly reviewed sanitized derivative. System-font paths are platform dependencies, not personal identities. Raw/cache/SQLite/large files remain excluded. New `.gitignore` rules also cover SQLite sidecars and `.part` files; `.gitattributes` preserves checksum-bound text bytes.

The scan uses high-signal secret patterns and does not export suspected values. Raw/cache/SQLite contents are not scanned for secrets, and there is no Git repository/history to audit in this workspace. External URLs are inventoried but not fetched. These limits must remain visible; a heuristic zero-hit result is not proof that secrets are absent.

The local Markdown/link and structure result is recorded in [preflight JSON](../data/release/phase8_release_preflight.json). Links to files excluded from the future public package are release holds even if they resolve locally. Recheck the exact staged package after sanitization.

Current local screen: zero high-signal secret hits, zero broken local Markdown paths, zero Python/JSON/CSV parse defects, and all seven ignore-rule fixtures pass. Twelve original files contain 40 machine-local path locations; two additional locations are system-font dependencies. Exact file counts/bytes and exclusions are machine-readable in the inventory, rather than frozen here while documentation is still being assembled.

## Report and visual contract

The selected audience is incentive/governance stakeholders. Per the explicit user request, the deliverable is repository-native Markdown, not a hosted app, dashboard or HTML report. The executive shape maps to: Executive Summary; evidence-led findings; recommendation; further questions; limitations. The short memo narrows the decision framing rather than duplicating the README.

All six existing figures are retained unchanged. Their chart contracts are in `docs/PHASE7_ANALYSIS_QA.md`. README paragraphs explain the takeaway and caveat next to each chart, distinguish full P180 balances from uplift, and flag connecting lines between sparse post checkpoints as guides rather than observed daily trajectories. Portfolio loan assets, collateral native units, primary versus peak denominators, cross-sectional borrower counts and market-only concentration remain separate.

`scripts/build_release_preview.cjs` built nine Markdown pages with Node 24.19.0 / marked 17.0.5, embedded all six checksum-recorded PNGs, required non-empty alt text and emitted [build evidence](../data/release/phase8_markdown_build.json). Temporary HTML is ignored QA output, not a public app/site. No external resources were requested. The X draft texts contain 202/200/174 Unicode codepoints before alias/URL substitution; this is not a final platform length certification.

**Rendered-preview limitation:** browser connection failed, and the subsequent local-file preview was rejected by browser URL policy. No workaround was attempted after that block. This session does not claim browser-render or exact GitHub-renderer visual signoff. Accepted Phase 7 figure review remains valid; review the assembled README/report in the final target renderer before publication.

## Required caveats and next gate

The observational design, absence of recipient attribution, price layer and wallet cohorts/concentration, unexercised non-zero fee shares, mature/late-market comparability and source-access limitations are in [release limitations](RELEASE_LIMITATIONS.md). Three bad-debt events are empirically covered.

At the original RC stop point independent review had not occurred. It subsequently returned `CHANGES REQUESTED`; the revision now awaits re-review under the [reviewer brief](REVIEWER_BRIEF.md) and [release checklist](RELEASE_CHECKLIST.md). Phase 8 is not closed.
